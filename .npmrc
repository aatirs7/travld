# Expo/Metro + React Native need a flat-ish, non-hoisted layout to resolve
# native peer deps reliably inside a pnpm monorepo. node-linker=hoisted keeps
# node_modules layout close to npm/yarn so Metro's resolver behaves.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
